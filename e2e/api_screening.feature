# language: zh-CN

功能: POST /api/v1/screen 筛选公司API

  背景:
    假设 API服务已启动
    并且 Redis缓存服务已连接
    并且 用户已获取有效的API认证令牌

  场景大纲: 单条件筛选
    假如 用户发送POST请求到 "/api/v1/screen"
    并且 请求体包含条件 "<指标>" "<操作符>" "<值>"
    当 系统处理筛选请求
    那么 响应状态码应为 200
    并且 响应中应包含符合条件的企业列表
    并且 每个结果的企业代码应格式正确

    例子:
      | 指标         | 操作符 | 值         |
      | 市盈率       | gt    | 50         |
      | 市净率       | lt    | 2          |
      | 每股收益     | gte   | 0.5        |
      | 负债率       | lte   | 60         |
      | 上市板块     | eq    | 主板       |
      | 总市值       | between | 10000000000,50000000000 |

  场景大纲: 多条件AND组合筛选
    假如 用户发送POST请求到 "/api/v1/screen"
    并且 请求体包含多个筛选条件使用AND逻辑
      | 指标         | 操作符 | 值          |
      | 净利润       | gt    | 100000000   |
      | 营收增长率   | gt    | 15          |
    并且 条件组合方式为 "and"
    当 系统处理筛选请求
    那么 响应状态码应为 200
    并且 所有返回的企业应同时满足两个条件

  场景大纲: 多条件OR组合筛选
    假如 用户发送POST请求到 "/api/v1/screen"
    并且 请求体包含多个筛选条件使用OR逻辑
      | 指标           | 操作符 | 值         |
      | 净利润增长率   | gt    | 50         |
      | 营业收入增长率 | gt    | 30         |
    并且 条件组合方式为 "or"
    当 系统处理筛选请求
    那么 响应状态码应为 200
    并且 返回的企业应满足至少一个条件

  场景大纲: 年度时间范围筛选
    假如 用户发送POST请求到 "/api/v1/screen"
    并且 请求体包含时间范围类型为 "年度"
    并且 请求体包含年度数为 "<年度数>"
    当 系统处理筛选请求
    那么 响应状态码应为 200
    并且 返回的数据应基于指定的年度范围

    例子:
      | 年度数 |
      | 1      |
      | 3      |
      | 5      |
      | 10     |

  场景大纲: 季度时间范围筛选
    假如 用户发送POST请求到 "/api/v1/screen"
    并且 请求体包含时间范围类型为 "季度"
    并且 请求体包含季度数为 "<季度数>"
    当 系统处理筛选请求
    那么 响应状态码应为 200
    并且 返回的数据应基于指定的季度范围

    例子:
      | 季度数 |
      | 1      |
      | 4      |
      | 8      |
      | 20     |

  场景大纲: 排序参数测试
    假如 用户发送POST请求到 "/api/v1/screen"
    并且 请求体包含排序字段为 "<排序字段>"
    并且 请求体包含排序方向为 "<排序方向>"
    当 系统处理筛选请求
    那么 响应状态码应为 200
    并且 返回结果应按指定字段和方向排序

    例子:
      | 排序字段     | 排序方向 |
      | 市盈率       | asc     |
      | 市盈率       | desc    |
      | 净利润       | asc     |
      | 净利润       | desc    |
      | 营业收入     | asc     |
      | 营业收入     | desc    |
      | 总市值       | asc     |
      | 总市值       | desc    |

  场景大纲: 分页参数测试
    假如 用户发送POST请求到 "/api/v1/screen"
    并且 请求体包含分页大小为 "<分页大小>"
    并且 请求体包含页码为 "<页码>"
    当 系统处理筛选请求
    那么 响应状态码应为 200
    并且 响应应包含分页信息
    并且 返回的记录数不应超过分页大小

    例子:
      | 分页大小 | 页码 |
      | 10       | 1    |
      | 20       | 1    |
      | 50       | 1    |
      | 100      | 1    |
      | 50       | 2    |
      | 50       | 3    |

  场景大纲: 行业过滤-包含指定行业
    假如 用户发送POST请求到 "/api/v1/screen"
    并且 请求体包含行业列表为 "<行业列表>"
    并且 过滤模式为 "include"
    当 系统处理筛选请求
    那么 响应状态码应为 200
    并且 返回的所有企业应属于指定行业

    例子:
      | 行业列表           |
      | 医药生物           |
      | 电子元件           |
      | 软件开发,互联网服务 |
      | 银行,保险,证券     |

  场景大纲: 行业过滤-排除指定行业
    假如 用户发送POST请求到 "/api/v1/screen"
    并且 请求体包含排除行业列表为 "<排除行业>"
    并且 过滤模式为 "exclude"
    当 系统处理筛选请求
    那么 响应状态码应为 200
    并且 返回的所有企业不应属于排除行业

    例子:
      | 排除行业 |
      | 银行     |
      | 房地产   |
      | 煤炭,钢铁 |

  场景: 排除停牌公司
    假如 用户发送POST请求到 "/api/v1/screen"
    并且 请求体包含筛选条件
    并且 参数 "include_suspended" 为 "false"
    当 系统处理筛选请求
    那么 响应状态码应为 200
    并且 返回结果不应包含当前停牌的企业

  场景: 仅包含盈利公司
    假如 用户发送POST请求到 "/api/v1/screen"
    并且 参数 "profit_only" 为 "true"
    当 系统处理筛选请求
    那么 响应状态码应为 200
    并且 返回的所有企业应为本周期盈利状态

  场景: 排除ST公司
    假如 用户发送POST请求到 "/api/v1/screen"
    并且 参数 "include_st" 为 "false"
    当 系统处理筛选请求
    那么 响应状态码应为 200
    并且 返回结果不应包含ST或*ST企业

  场景: 要求完整数据
    假如 用户发送POST请求到 "/api/v1/screen"
    并且 参数 "require_complete_data" 为 "true"
    当 系统处理筛选请求
    那么 响应状态码应为 200
    并且 返回的企业应包含所有必需财务指标数据

  场景大纲: 组合排除条件
    假如 用户发送POST请求到 "/api/v1/screen"
    并且 参数 "exclude_industry" 包含 "<排除行业>"
    并且 参数 "include_suspended" 为 "false"
    并且 参数 "profit_only" 为 "true"
    并且 参数 "include_st" 为 "false"
    当 系统处理筛选请求
    那么 响应状态码应为 200
    并且 返回结果应同时满足所有排除条件

    例子:
      | 排除行业 |
      | 银行     |
      | 房地产   |

  场景: 综合筛选场景
    假如 用户发送POST请求到 "/api/v1/screen"
    并且 请求体包含以下筛选条件:
      | 指标         | 操作符 | 值          |
      | 市盈率       | between | 10,50      |
      | 净利润       | gt     | 100000000   |
      | 营收增长率   | gt     | 10          |
    并且 时间范围类型为 "年度"
    并且 年度数为 "3"
    并且 排序字段为 "净利润"
    并且 排序方向为 "desc"
    并且 分页大小为 "20"
    并且 页码为 "1"
    并且 参数 "profit_only" 为 "true"
    并且 参数 "include_st" 为 "false"
    当 系统处理筛选请求
    那么 响应状态码应为 200
    并且 返回结果数量不应超过20
    并且 返回的企业应按净利润降序排列
    并且 所有企业应满足市盈率10-50范围且盈利

  场景: 无符合条件结果
    假如 用户发送POST请求到 "/api/v1/screen"
    并且 请求体包含不可能满足的条件:
      | 指标     | 操作符 | 值     |
      | 市盈率   | lt    | -100   |
    当 系统处理筛选请求
    那么 响应状态码应为 200
    并且 返回的企业列表应为空

  场景: 缺少必需参数
    假如 用户发送POST请求到 "/api/v1/screen"
    并且 请求体为空对象
    当 系统处理筛选请求
    那么 响应状态码应为 422
    并且 响应应包含参数验证错误信息

  场景: 无效的排序字段
    假如 用户发送POST请求到 "/api/v1/screen"
    并且 请求体包含无效的排序字段 "不存在的字段"
    当 系统处理筛选请求
    那么 响应状态码应为 400
    并且 响应应包含字段验证错误信息

  场景: 分页超出范围
    假如 用户发送POST请求到 "/api/v1/screen"
    并且 请求体包含页码为 "999999"
    当 系统处理筛选请求
    那么 响应状态码应为 200
    并且 返回的企业列表应为空
