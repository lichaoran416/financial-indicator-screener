# language: zh-CN
功能: API请求日志记录

  背景: 
    假设 后端服务已启动并配置日志系统
    并且 日志级别设置为DEBUG
    并且 结构化JSON日志输出已启用

  场景: 记录GET请求的完整信息
    当 客户端发送GET请求到"/api/v1/company/600519"
    那么 日志应记录请求方法为"GET"
    并且 日志应记录请求路径为"/api/v1/company/600519"
    并且 日志应记录请求时间戳
    并且 日志应包含唯一请求ID

  场景: 记录POST请求的参数信息
    当 客户端发送POST请求到"/api/v1/screen"包含筛选条件
    令 请求参数为{"pe_ratio":{"min":10,"max":20},"roe":{"min":15}}"
    那么 日志应记录请求方法为"POST"
    并且 日志应记录完整的请求参数
    并且 日志应记录请求体大小

  场景: 记录请求处理时长
    当 客户端发送请求到"/api/v1/screen"
    当 请求处理完成
    那么 日志应记录请求处理时长（毫秒）
    并且 时长精度应达到毫秒级

  场景: 记录用户行为用于调试
    当 用户在60秒内连续发起3次相同请求
    那么 日志应记录重复请求警告
    并且 日志应包含请求间隔时间
