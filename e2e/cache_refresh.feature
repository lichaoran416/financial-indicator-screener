# language: zh-CN
功能: 缓存刷新

  背景: 
    假设 Redis 缓存服务已启动
    并且 系统中已缓存多个股票数据

  场景: 用户强制刷新所有缓存数据
    当 用户调用 POST /api/v1/cache/refresh 接口
    那么 系统应清除所有现有的缓存键
    并且 响应应返回被清除的键数量
    并且 响应状态码应为 200

  场景: 强制刷新后首次请求触发数据重新缓存
    假设 用户已执行强制刷新缓存操作
    当 用户请求获取股票 "000001" 的分析数据
    那么 系统应从 akshare 获取最新数据
    并且 系统应将新数据存入缓存

  场景: 用户需要最新财务数据时手动刷新
    假设 当前时间为财报发布后
   并且 缓存中存有旧的财务数据
    当 用户手动触发强制刷新
    那么 系统应清除所有旧缓存数据
   并且 后续请求应获取到最新的财务数据
