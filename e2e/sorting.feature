# language: zh-CN
功能: 筛选结果排序 - JTB-002

  背景:
    假设 用户已完成一次筛选操作
    且 筛选结果已显示在页面上
    且 当前筛选结果包含至少5家企业

  @JTB-002-01
  场景: 按主指标升序排序
    假设 用户在筛选结果页面
    当 用户选择排序指标为 "净资产收益率"
    且 用户选择排序方式为 "升序"
    当 用户点击"排序"按钮
    那么 结果应按净资产收益率从小到大排列
    且 矩形树图应相应更新显示顺序

  @JTB-002-02
  场景: 按主指标降序排序
    假设 用户在筛选结果页面
    当 用户选择排序指标为 "净利润"
    且 用户选择排序方式为 "降序"
    当 用户点击"排序"按钮
    那么 结果应按净利润从大到小排列
    且 矩形树图应相应更新显示顺序

  @JTB-002-03
  场景: 主次排序组合
    假设 用户在筛选结果页面
    当 用户选择主排序指标为 "营业收入"
    且 用户选择主排序方式为 "降序"
    且 用户选择次排序指标为 "净利润"
    且 用户选择次排序方式为 "降序"
    当 用户点击"排序"按钮
    那么 结果应先按营业收入降序排列
    且 营业收入相同时按净利润降序排列

  @JTB-002-04
  场景: 主指标升序次指标降序
    假设 用户在筛选结果页面
    当 用户选择主排序指标为 "市盈率"
    且 用户选择主排序方式为 "升序"
    且 用户选择次排序指标为 "市值"
    且 用户选择次排序方式为 "降序"
    当 用户点击"排序"按钮
    那么 结果应先按市盈率升序排列
    且 市盈率相同时按市值降序排列

  @JTB-002-05
  场景: 矩形树图自动按指标值排序
    假设 筛选结果以矩形树图显示
    当 用户未手动设置排序
    那么 矩形树图应自动按指标值降序排列
    且 最大的矩形应显示指标值最高的企业

  @JTB-002-06
  场景: 使用后台sort_by参数排序
    假设 用户配置筛选条件
    当 用户设置排序参数:
      | 参数 | 值 |
      | sort_by | 净资产收益率 |
      | order | desc |
      | sort_by_2 | 净利润 |
      | order_2 | desc |
    当 用户点击"开始筛选"按钮
    那么 系统应调用 "POST /api/v1/screen" 接口并传递排序参数
    且 响应结果应按指定排序返回

  @JTB-002-07
  场景: 升序排列时相同值处理
    假设 用户在筛选结果页面
    当 用户选择排序指标为 "资产负债率"
    且 用户选择排序方式为 "升序"
    当 用户点击"排序"按钮
    那么 资产负债率相同的企业应相邻显示
    且 相同值的企业间顺序应保持稳定

  @JTB-002-08
  场景: 切换排序指标后重置次排序
    假设 用户已设置主次排序指标
    当 用户更改主排序指标
    那么 次排序指标应重置为未选择状态
    且 用户需要重新选择次排序指标

  @JTB-002-09
  场景: 排序后分页保持
    假设 筛选结果超过一页显示
   且 用户当前在第2页
    当 用户设置排序条件
    当 用户点击"排序"按钮
    那么 用户应保持在第2页
    且 当前页结果应已排序

  @JTB-002-10
  场景: 清除排序恢复默认
    假设 用户已设置排序条件
    当 用户点击"清除排序"按钮
    那么 所有排序条件应被清除
    且 结果应恢复为默认排序顺序
