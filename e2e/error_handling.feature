# language: zh-CN
功能: 用户友好的错误处理

  背景:
    假设 前端应用运行在 "http://localhost:3000"
    假设 后端API运行在 "http://localhost:8000"

  场景: 网络错误显示重试按钮
    假设 网络连接已断开
    当 用户访问筛选页面
    且 用户点击"开始筛选"按钮
    那么 系统应显示错误提示 "网络连接失败"
    且 界面应显示"重试"按钮
    当 用户点击"重试"按钮
    那么 系统应重新发送筛选请求

  场景: 不存在的数据显示友好的空状态消息
    当 用户发送筛选条件查询一个不存在的股票代码
      """
      {
        "conditions": [
          {"metric": "code", "operator": "eq", "value": "SH999999"}
        ]
      }
      """
    那么 响应状态码应为 200
    且 响应包含空结果集
    且 前端应显示友好消息 "未找到符合条件的股票"

  场景: 计算超时提示简化条件
    假设 筛选条件过于复杂导致计算超时
    当 用户发送包含大量条件的筛选请求
    且 计算时间超过 30 秒
    那么 系统应返回超时错误
    且 错误消息应提示 "查询条件过于复杂，请尝试简化筛选条件"
    且 建议包括 "减少指标数量" 或 "扩大数值范围"

  场景: 缓存命中时筛选响应时间小于2秒
    假设 缓存中已存在筛选结果
    当 用户发送相同的筛选条件
    那么 响应时间应小于 2000 毫秒
    且 响应状态码应为 200

  场景: 首次计算时筛选响应时间小于10秒
    假设 缓存中不存在筛选结果
    当 用户发送新的筛选条件
    那么 响应时间应小于 10000 毫秒
    且 响应状态码应为 200

  场景: 页面加载时间小于1秒
    当 用户首次访问筛选页面
    那么 页面完全加载时间应小于 1000 毫秒
    且 主要内容应可见

  场景: API服务不可用时显示服务暂不可用
    假设 后端API服务已停止
    当 用户点击"开始筛选"按钮
    Then 系统应显示错误提示 "服务暂不可用，请稍后重试"
    And 界面不应崩溃

  场景: 验证错误显示具体字段问题
    当 用户提交无效的筛选条件
    那么 错误消息应明确指出问题字段
    And 错误消息应使用用户可理解的语言
    And 应提供正确的格式示例
