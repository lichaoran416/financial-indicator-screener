# language: zh-CN
功能: 缓存管理

  背景: 
    假设 Redis 缓存服务已启动
    并且 缓存 TTL 设置为 24 小时
   并且 系统使用 akshare 获取财务数据

  场景: 缓存命中时快速返回结果
    假设 数据库中已存在股票代码 "000001" 的缓存数据
    当 用户请求获取股票 "000001" 的分析数据
    那么 系统应从缓存返回数据
    并且 响应时间应小于 2 秒

  场景: 缓存未命中时从 akshare 获取数据
    假设 缓存中不存在股票代码 "000002" 的数据
    当 用户请求获取股票 "000002" 的分析数据
    那么 系统应调用 akshare 接口获取数据
    并且 系统应将获取的数据存储到 Redis 缓存
    并且 返回给用户的数据应包含完整的财务指标

  场景: 缓存数据在 TTL 过期后被清除
    假设 缓存中已存在股票代码 "000003" 的数据
    并且 该缓存数据已超过 24 小时 TTL
    当 用户请求获取股票 "000003" 的分析数据
    那么 系统应视为缓存未命中
    并且 系统应从 akshare 重新获取数据
