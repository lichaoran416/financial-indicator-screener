# language: zh-CN
功能: 请求追踪

  背景: 
    假设 后端服务已启动
    并且 请求追踪系统已启用

  场景: 为每个请求生成唯一UUIDv4请求ID
    当 客户端发送新的HTTP请求
    那么 系统应生成UUIDv4格式的请求ID
    并且 请求ID应记录到响应头"X-Request-ID"

  场景: 在整个请求生命周期中传递请求ID
    当 处理一个HTTP请求时
    那么 请求ID应从请求入口传递到日志系统
    并且 所有相关日志都应包含相同的请求ID
    并且 请求ID应传递到子服务调用

  场景: 追踪单个请求的所有关联日志
    当 处理请求期间产生多条日志
    那么 所有日志应包含相同的request_id
    并且 可以通过request_id过滤出该请求的所有日志

  场景: 记录请求生命周期完整轨迹
    当 处理一个包含多个步骤的请求
    那么 日志应按顺序记录:
      | 步骤 | 说明 |
      | 请求接收 | 记录请求开始 |
      | 参数解析 | 记录解析结果 |
      | 缓存查询 | 记录缓存命中情况 |
      | 数据获取 | 记录数据源响应 |
      | 数据处理 | 记录处理结果 |
      | 响应发送 | 记录请求完成 |

  场景: 请求ID在并发情况下的唯一性
    当 同时处理100个并发请求
    那么 每个请求应有不同的request_id
    并且 不应出现request_id重复的情况

  场景: 支持分布式追踪
    当 请求需要调用多个微服务
    那么 所有服务应使用相同的request_id
    并且 可以通过request_id串联完整调用链
