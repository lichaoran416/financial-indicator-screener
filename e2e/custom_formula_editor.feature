# language: zh-CN
功能: JTB-005 创建自定义财务指标

  背景:
    假设 用户已登录系统
    并且 用户已进入自定义公式编辑器页面

  场景: 使用基础运算符创建公式
    假如 用户在公式输入框中输入 "ROE + ROA"
    当 用户点击预览按钮
    那么 系统应显示计算结果
    并且 公式应被正确解析为 ROE 与 ROA 的和

  场景: 使用减法运算符计算差值
    假如 用户在公式输入框中输入 "净利润 - 营业成本"
    当 用户点击预览按钮
    那么 系统应显示计算结果
    并且 公式应被正确解析为净利润与营业成本的差

  场景: 使用乘法和除法运算符
    假如 用户在公式输入框中输入 "总收入 / 总资产 * 100"
    当 用户点击预览按钮
    那么 系统应显示计算结果
    并且 公式应正确反映 (总收入 / 总资产) * 100 的计算

  场景: 使用括号明确运算优先级
    假如 用户在公式输入框中输入 "(ROE + ROA) / 总资产 * 100"
    当 用户点击预览按钮
    那么 系统应先计算 ROE + ROA 的和
    并且 再除以总资产后乘以100
    并且 显示最终计算结果

  场景: 使用括号改变默认运算优先级
    假如 用户在公式输入框中输入 "ROE + ROA / 总资产"
    但是 用户未使用括号指定优先级
    当 用户点击预览按钮
    那么 系统应按照默认优先级计算 (ROA / 总资产) + ROE
    并且 在公式预览区显示优先级说明

  场景: 使用 AVG 函数计算平均值
    假如 用户在公式输入框中输入 "AVG(ROE)"
    当 用户点击预览按钮
    那么 系统应显示 ROE 的平均值
    并且 默认计算所有可用期间的平均值

  场景: 使用 SUM 函数计算总和
    假如 用户在公式输入框中输入 "SUM(净利润)"
    当 用户点击预览按钮
    那么 系统应显示净利润的总和

  场景: 使用 MIN 函数获取最小值
    假如 用户在公式输入框中输入 "MIN(ROE)"
    当 用户点击预览按钮
    那么 系统应显示 ROE 的最小值

  场景: 使用 MAX 函数获取最大值
    假如 用户在公式输入框中输入 "MAX(ROA)"
    当 用户点击预览按钮
    那么 系统应显示 ROA 的最大值

  场景: 使用 STD 函数计算标准差
    假如 用户在公式输入框中输入 "STD(ROE)"
    当 用户点击预览按钮
    那么 系统应显示 ROE 的标准差

  场景: 使用时间序列语法获取特定年份数据
    假如 用户在公式输入框中输入 "ROE[2023]"
    当 用户点击预览按钮
    那么 系统应显示2023年的 ROE 数据
    并且 不应包含其他年份的数据

  场景: 使用时间序列语法获取年份范围数据
    假如 用户在公式输入框中输入 "AVG(ROE[2020:2024])"
    当 用户点击预览按钮
    那么 系统应计算2020年至2024年ROE的平均值
    并且 每个年份的数据都应被正确获取

  场景: 组合使用时间序列和聚合函数
    假如 用户在公式输入框中输入 "SUM(净利润[2019:2023]) / AVG(总资产[2019:2023])"
    当 用户点击预览按钮
    那么 系统应计算2019-2023年净利润总和除以总资产平均值

  场景: 语法错误 - 缺少右括号
    假如 用户在公式输入框中输入 "AVG(ROE"
    当 用户点击预览按钮
    那么 系统应显示错误提示 "公式语法错误：缺少右括号"
    并且 高亮显示缺失括号的位置

  场景: 语法错误 - 未知函数名
    假如 用户在公式输入框中输入 "MEAN(ROE)"
    当 用户点击预览按钮
    那么 系统应显示错误提示 "未知函数：MEAN，请使用 AVG、SUM、MIN、MAX 或 STD"
    并且 不执行计算

  场景: 语法错误 - 无效的时间序列格式
    假如 用户在公式输入框中输入 "ROE[2023"
    当 用户点击预览按钮
    那么 系统应显示错误提示 "时间序列语法错误：应使用 ROE[2023] 或 ROE[2020:2024] 格式"
    并且 不执行计算

  场景: 语法错误 - 无效的年份范围
    假如 用户在公式输入框中输入 "ROE[2024:2020]"
    当 用户点击预览按钮
    那么 系统应显示错误提示 "年份范围错误：起始年份应小于结束年份"
    并且 不执行计算

  场景: 语法错误 - 空公式
    假如 用户在公式输入框中保留空白
    当 用户点击预览按钮
    那么 系统应显示错误提示 "请输入公式"
    并且 预览按钮保持可用状态

  场景: 保存公式 - 完整信息
    假如 用户在公式输入框中输入 "ROE + ROA"
    并且 用户在公式名称输入框中输入 "综合盈利能力指标"
    并且 用户在描述输入框中输入 "用于计算综合盈利能力的自定义指标"
    当 用户点击保存按钮
    那么 系统应保存公式
    并且 显示保存成功提示
    并且 公式应出现在已保存公式列表中

  场景: 保存公式 - 仅名称
    假如 用户在公式输入框中输入 "净利润 / 营业收入 * 100"
    并且 用户在公式名称输入框中输入 "净利率"
    当 用户点击保存按钮
    那么 系统应保存公式
    并且 显示保存成功提示

  场景: 保存公式 - 缺少名称
    假如 用户在公式输入框中输入 "ROE / ROA"
    但是 用户未填写公式名称
    当 用户点击保存按钮
    那么 系统应显示错误提示 "请输入公式名称"
    并且 公式不应被保存

  场景: 保存公式 - 名称重复
    假如 用户尝试保存名为 "综合盈利能力指标" 的公式
    但是 该名称的公式已存在
    当 用户点击保存按钮
    那么 系统应显示错误提示 "该名称已存在，请使用其他名称"
    并且 不覆盖原有公式

  场景: 编辑已保存的公式
    假如 用户选择已保存的公式 "综合盈利能力指标"
    当 用户修改公式为 "ROE - ROA"
    并且 用户点击更新按钮
    那么 系统应更新公式
    并且 显示更新成功提示

  场景: 删除已保存的公式
    假如 用户选择已保存的公式 "净利率"
    当 用户点击删除按钮
    并且 用户在确认对话框中点击确定
    那么 系统应删除该公式
    并且 显示删除成功提示
    并且 该公式不应再出现在列表中

  场景: 公式名称长度限制
    假如 用户在公式名称输入框中输入超过50个字符的名称
    当 用户尝试保存
    那么 系统应显示错误提示 "公式名称不能超过50个字符"
    并且 不保存公式

  例子: 各种运算符组合测试
    | 公式表达式              | 预期结果类型 |
    | "ROE + ROA"            | 数值         |
    | "净利润 - 营业成本"     | 数值         |
    | "收入 * 利润率"         | 数值         |
    | "净资产 / 总资产"       | 数值         |
    | "(A + B) * C"          | 数值         |
    | "AVG(ROE[2020:2024])"  | 数值         |
    | "SUM(净利润) / COUNT(净利润)" | 数值 |

  例子: 时间序列语法测试
    | 公式表达式              | 时间范围     | 说明                 |
    | "ROE[2023]"            | 单一年份     | 获取2023年数据       |
    | "ROE[2020:2024]"       | 连续年份     | 获取2020-2024年数据  |
    | "净利润[2023]"         | 单一年份     | 获取2023年净利润     |
    | "AVG(ROE[2019:2023])"  | 聚合多年     | 计算5年平均ROE       |

  例子: 内置函数测试
    | 函数   | 输入参数    | 预期行为                 |
    | AVG    | ROE         | 计算ROE的平均值          |
    | SUM    | 净利润      | 计算净利润的总和          |
    | MIN    | ROA         | 获取ROA的最小值          |
    | MAX    | ROE         | 获取ROE的最大值          |
    | STD    | ROE         | 计算ROE的标准差          |
