# language: zh-CN
功能: POST /api/v1/formula/evaluate 公式计算API
  作为API调用者
  我希望能够通过公式计算接口进行各种数学运算
  以便对股票数据进行复杂的公式计算和验证

  背景:
    假设 API 服务运行在 "http://localhost:8000"
    并且 API 路径为 "/api/v1/formula/evaluate"
    并且 请求头包含 "Content-Type: application/json"

  场景: 成功计算返回结果
    假如 请求体包含公式 "PE > 10"
    并且 请求体包含公司代码 "000001"
    并且 请求体包含必要的时间范围
    当 发送 POST 请求到公式计算接口
    那么 响应状态码应为 200
    并且 响应体应包含 "result" 字段
    并且 响应体应包含 "success" 字段值为 true

  场景: 缺少metrics数据处理
    假如 请求体包含公式 "ROE > 0.1"
    但是 请求体中缺少必要的 metrics 数据
    当 发送 POST 请求到公式计算接口
    那么 响应状态码应为 400
    或者 响应状态码应为 422
    并且 响应体应包含错误信息描述

  场景: 除零错误处理
    假如 请求体包含公式 "Revenue / NetProfit"
    并且 公司的 NetProfit 值为 0
    当 发送 POST 请求到公式计算接口
    那么 响应状态码应为 200
    并且 响应体应包含错误信息指示除零问题
    或者 响应结果应为 null 或 infinity

  场景: 大数值计算
    假如 请求体包含公式 "MarketCap * 1000000"
    并且 公司市值数据为大数值
    当 发送 POST 请求到公式计算接口
    那么 响应状态码应为 200
    并且 响应体应包含正确计算的大数值结果

  场景: 负数计算
    假如 请求体包含公式 "TotalAssets - TotalLiabilities"
    并且 公司的某些数据包含负值
    当 发送 POST 请求到公式计算接口
    那么 响应状态码应为 200
    并且 响应体应包含正确的负数计算结果

  场景: 时间序列公式计算(ROE[2020:2024])
    假如 请求体包含公式 "ROE[2020:2024]"
    并且 请求体包含公司代码 "000001"
    当 发送 POST 请求到公式计算接口
    那么 响应状态码应为 200
    并且 响应体应包含 2020 到 2024 年的时间序列数据
    并且 每个年份应包含对应的 ROE 值
