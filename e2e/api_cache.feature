# language: zh-CN

功能: POST /api/v1/cache/refresh 缓存刷新API
  作为用户
  我想要强制刷新所有缓存数据
  以便获取最新的股票数据

  背景:
    假如 系统已启动并连接到Redis缓存
    并且 缓存中已存在部分股票数据

  场景: 强制刷新所有缓存
    假如 用户发起强制刷新缓存请求
    当 系统接收到 POST /api/v1/cache/refresh 请求
    那么 系统应清除所有现有的缓存key
    并且 系统应返回刷新成功的响应

  场景: 返回清除的key数量
    假如 缓存中存在多个股票数据的key
    当 用户发起强制刷新缓存请求
    那么 响应应包含清除的key数量信息
    并且 key数量应与实际清除的数量一致

  场景: 缓存TTL验证(24小时)
    假如 缓存已被刷新清除
    当 系统重新缓存股票数据
    那么 新缓存的TTL应为24小时
    并且 缓存数据应在24小时后自动过期

  场景: 刷新时新请求处理
    假如 系统正在执行缓存刷新操作
    当 用户发起新的股票查询请求
    那么 系统应正常处理新请求
    并且 新请求应访问数据源而非返回过期的缓存数据

  场景: 并发刷新请求处理
    假如 缓存中存在数据
    当 多个用户同时发起缓存刷新请求
    那么 系统应正确处理并发刷新
    并且 只会执行一次实际的刷新操作
    或者 返回适当的错误或警告信息防止重复刷新

  例子: 缓存刷新响应格式
    | 场景           | 预期结果状态码 |
    | 刷新成功       | 200            |
    | 并发刷新       | 200 或 409     |
    | Redis未连接    | 500            |
