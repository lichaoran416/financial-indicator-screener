# language: zh-CN
功能: 财务指标筛选 - JTB-001

  背景:
    假设 用户已登录系统
    且 系统数据已加载

  @JTB-001-01
  场景: 使用单一条件筛选上市公司
    假设 用户进入筛选页面
    当 用户选择指标 "净利润增长率"
    且 用户选择时间范围为 "近1年"
    且 用户选择运算符 "大于"
    且 用户输入数值 "10"
    当 用户点击"开始筛选"按钮
    那么 系统应显示加载动画
    且 系统应调用 "POST /api/v1/screen" 接口
    且 响应状态码应为 "200"
    且 结果应显示符合条件的企业列表
    且 结果应以矩形树图形式展示

  @JTB-001-02
  场景: 使用AND逻辑组合多个条件
    假设 用户进入筛选页面
    当 用户添加第一个条件:
      | 指标 | 运算符 | 值 |
      | 净资产收益率 | 大于 | 15 |
    当 用户点击"添加条件"按钮
    当 用户添加第二个条件:
      | 指标 | 运算符 | 值 |
      | 资产负债率 | 小于 | 50 |
    当 用户选择逻辑关系为 "AND"
    当 用户点击"开始筛选"按钮
    那么 系统应返回同时满足两个条件的企业
    且 结果中每个企业都满足条件1和条件2

  @JTB-001-03
  场景: 使用OR逻辑组合多个条件
    假设 用户进入筛选页面
    当 用户添加第一个条件:
      | 指标 | 运算符 | 值 |
      | 市盈率 | 小于 | 20 |
    当 用户点击"添加条件"按钮
    当 用户添加第二个条件:
      | 指标 | 运算符 | 值 |
      | 市净率 | 小于 | 2 |
    当 用户选择逻辑关系为 "OR"
    当 用户点击"开始筛选"按钮
    那么 系统应返回满足任一条件的企业

  @JTB-001-04
  场景: 使用between运算符筛选范围
    假设 用户进入筛选页面
    当 用户选择指标 "每股收益"
    且 用户选择时间范围为 "近2年"
    且 用户选择运算符 "介于"
    且 用户输入范围 "0.5-2.0"
    当 用户点击"开始筛选"按钮
    那么 系统应返回每股收益在0.5到2.0之间的企业

  @JTB-001-05
  场景: 使用自定义公式筛选
    假设 用户进入筛选页面
    当 用户选择"自定义公式"模式
    当 用户输入公式 "(净利润 / 营业收入) * 100"
    且 用户选择时间范围为 "近1年"
    且 用户选择运算符 "大于"
    且 用户输入数值 "10"
    当 用户点击"开始筛选"按钮
    那么 系统应解析并计算自定义公式
    且 系统应返回计算结果大于10的企业

  @JTB-001-06
  场景: 使用带括号的复杂公式
    假设 用户进入筛选页面
    当 用户选择"自定义公式"模式
    当 用户输入公式 "(净资产 - 无形资产) / 总资产"
    且 用户选择时间范围为 "近1年"
    且 用户选择运算符 "大于"
    且 用户输入数值 "0.3"
    当 用户点击"开始筛选"按钮
    那么 系统应正确解析括号优先级
    且 系统应返回计算结果大于0.3的企业

  @JTB-001-07
  场景: 选择季度时间范围筛选
    假设 用户进入筛选页面
    当 用户选择指标 "营业收入"
    且 用户选择时间范围为 "近8个季度"
    且 用户选择运算符 "大于"
    且 用户输入数值 "100000000"
    当 用户点击"开始筛选"按钮
    那么 系统应返回最近8个季度数据符合条件的企业

  @JTB-001-08
  场景: 使用大于等于运算符
    假设 用户进入筛选页面
    当 用户选择指标 "市值"
    且 用户选择时间范围为 "近1年"
    且 用户选择运算符 "大于等于"
    且 用户输入数值 "10000000000"
    当 用户点击"开始筛选"按钮
    那么 系统应返回市值大于等于100亿元的企业

  @JTB-001-09
  场景: 使用小于等于运算符
    假设 用户进入筛选页面
    当 用户选择指标 "市盈率"
    且 用户选择时间范围为 "近1年"
    且 用户选择运算符 "小于等于"
    且 用户输入数值 "15"
    当 用户点击"开始筛选"按钮
    那么 系统应返回市盈率小于等于15的企业

  @JTB-001-10
  场景: 使用等于运算符
    假设 用户进入筛选页面
    当 用户选择指标 "上市状态"
    且 用户选择时间范围为 "当前"
    且 用户选择运算符 "等于"
    且 用户输入值 "主板"
    当 用户点击"开始筛选"按钮
    那么 系统应返回主板上市的企业

  @JTB-001-11
  场景: 筛选结果以矩形树图展示
    假设 用户已完成筛选条件配置
    当 用户点击"开始筛选"按钮
    那么 系统应显示矩形树图
    且 树图中矩形大小应与指标值成正比
    且 树图中矩形颜色应区分不同企业
    且 用户可悬停查看企业详情

  @JTB-001-12
  场景: 筛选结果为空
    假设 用户进入筛选页面
    当 用户选择指标 "净利润增长率"
    且 用户选择时间范围为 "近1年"
    且 用户选择运算符 "大于"
    且 用户输入数值 "999999"
    当 用户点击"开始筛选"按钮
    那么 系统应显示"未找到符合条件的企业"
    且 系统应提示用户调整筛选条件
