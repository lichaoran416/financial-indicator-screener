# language: zh-CN
功能: 公式CRUD API

  背景:
    假如 系统正常运行
    并且 API服务已启动

  场景: 保存新公式
    假如 请求体包含以下公式数据:
      | 字段名 | 值 |
      | name | 测试公式 |
      | formula | 净利润 / 总资产 * 100 |
      | description | 计算净资产收益率 |
      | category | profitability |
    当 用户发送POST请求到 "/api/v1/formula/save"
    那么 响应状态码应为 200
    并且 响应体应包含字段 "id"
    并且 响应体中 "name" 应为 "测试公式"
    并且 响应体中 "formula" 应为 "净利润 / 总资产 * 100"
    并且 响应体中 "category" 应为 "profitability"

  场景: 获取已保存公式列表
    假如 已存在以下公式:
      | name | formula | category |
      | 公式A | 收入 - 成本 | profitability |
      | 公式B | 流动资产 / 流动负债 | liquidity |
    当 用户发送GET请求到 "/api/v1/formula/saved"
    那么 响应状态码应为 200
    并且 响应体应是一个数组
    并且 响应体应包含 "公式A"
    并且 响应体应包含 "公式B"

  场景: 更新已存在的公式
    假如 已存在公式ID为 "1" 的公式
    并且 请求体包含以下更新数据:
      | 字段名 | 值 |
      | name | 更新后的公式名称 |
      | formula | 新公式内容 |
      | description | 更新后的描述 |
    当 用户发送PUT请求到 "/api/v1/formula/1"
    那么 响应状态码应为 200
    并且 响应体中 "name" 应为 "更新后的公式名称"
    并且 响应体中 "formula" 应为 "新公式内容"

  场景: 删除已存在的公式
    假如 已存在公式ID为 "2" 的公式
    当 用户发送DELETE请求到 "/api/v1/formula/2"
    那么 响应状态码应为 200
    并且 响应体应包含成功信息

  场景: 删除不存在的公式时返回404
    假如 公式ID "99999" 不存在
    当 用户发送DELETE请求到 "/api/v1/formula/99999"
    那么 响应状态码应为 404
    并且 响应体应包含错误信息 "公式不存在"

  场景: 更新不存在的公式时返回404
    假如 公式ID "88888" 不存在
    并且 请求体包含以下更新数据:
      | 字段名 | 值 |
      | name | 不存在公式的名称 |
      | formula | 不存在的公式内容 |
    当 用户发送PUT请求到 "/api/v1/formula/88888"
    那么 响应状态码应为 404
    并且 响应体应包含错误信息 "公式不存在"

  场景: 完整的CRUD生命周期
    假如 请求体包含以下公式数据:
      | 字段名 | 值 |
      | name | 生命周期测试公式 |
      | formula | 总资产 / 总负债 |
      | description | 资产负债率计算 |
      | category | leverage |
    当 用户发送POST请求到 "/api/v1/formula/save"
    那么 响应状态码应为 200
    并且 响应体应包含新创建的公式ID，保存到变量 "createdId"

    当 用户发送GET请求到 "/api/v1/formula/saved"
    那么 响应状态码应为 200
    并且 响应体应包含刚创建的公式 "生命周期测试公式"

    假如 请求体包含以下更新数据:
      | 字段名 | 值 |
      | name | 生命周期测试公式-已更新 |
      | formula | 总负债 / 总资产 |
    当 用户发送PUT请求到 "/api/v1/formula/{createdId}"
    那么 响应状态码应为 200
    并且 响应体中 "name" 应为 "生命周期测试公式-已更新"

    当 用户发送DELETE请求到 "/api/v1/formula/{createdId}"
    那么 响应状态码应为 200

    当 用户发送GET请求到 "/api/v1/formula/{createdId}"
    那么 响应状态码应为 404

  场景: 保存公式时验证必填字段
    假如 请求体只包含以下数据:
      | 字段名 | 值 |
      | name | 无公式内容的公式 |
    当 用户发送POST请求到 "/api/v1/formula/save"
    那么 响应状态码应为 400
    并且 响应体应包含错误信息 "formula为必填字段"

  场景: 获取公式列表为空时返回空数组
    假如 不存在任何已保存的公式
    当 用户发送GET请求到 "/api/v1/formula/saved"
    那么 响应状态码应为 200
    并且 响应体应是一个空数组

  场景: 保存公式时名称重复
    假如 已存在名为 "重复名称" 的公式
    并且 请求体包含以下公式数据:
      | 字段名 | 值 |
      | name | 重复名称 |
      | formula | 新公式内容 |
    当 用户发送POST请求到 "/api/v1/formula/save"
    那么 响应状态码应为 409
    并且 响应体应包含错误信息 "公式名称已存在"
