# language: zh-CN
功能: 筛选条件可视化

  背景:
    假设 用户已登录系统
    而且 进入了条件设置页面
    而且 系统已加载所有可选指标

  场景: JTB-016-01 条件树状结构图
    假设 用户设置了多个筛选条件
    当 用户查看条件设置区域时
    那么 系统应以树状结构图展示所有条件
    而且 父节点应为逻辑运算符(AND/OR)
    而且 子节点应为具体筛选条件

  场景: JTB-016-02 数值范围滑块
    假设 用户设置了"市盈率大于10且小于50"的条件
    当 渲染条件设置界面时
    那么 系统应显示双端滑块组件
    而且 滑块范围应覆盖指标的合理取值区间
    而且 当前选中范围应在滑块上直观显示

  场景: JTB-016-03 行业分布热力图
    假设 用户已设置除行业外的其他条件
    当 用户点击"查看行业分布"时
    那么 系统应显示行业分布热力图
    而且 热力图颜色深浅应代表符合条件的企业数量
    而且 用户可点击热力图区块快速添加行业条件

  场景: JTB-016-04 AND逻辑可视化
    假设 用户添加了两个AND连接的条件
    当 展示条件树时
    那么 两个条件应以"AND"节点连接
   而且 视觉上应显示为上下或串联关系

  场景: JTB-016-05 OR逻辑可视化
    假设 用户添加了两个OR连接的条件
    当 展示条件树时
    那么 两个条件应以"OR"节点连接
    而且 视觉上应显示为并联或分支关系

  场景: JTB-016-06 组合条件的嵌套显示
    假设 用户设置了 (A AND B) OR (C AND D) 的条件组合
    当 系统渲染条件树时
    那么 应正确显示两层嵌套结构
    而且 内层为AND关系 外层为OR关系

  场景: JTB-016-07 条件节点拖拽调整
    假设 用户查看了条件树状结构图
    当 用户拖拽某个条件节点到新位置时
    那么 系统的逻辑关系应相应更新
    而且 树状结构图应重新渲染
